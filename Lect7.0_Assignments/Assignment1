#include <iostream>
using namespace std;

int SumN(int n) {
	if (n == 0 || n == 1) return 1;
	return n + SumN(n - 1);
}

int main() {
	int n;
	cin >> n;
	int sum = SumN(n);
	cout << sum << endl;
	return 0;
}
